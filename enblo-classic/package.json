{
  "name": "enblo-classic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "node scripts/build.mjs",
    "sim": "npx vite-node scripts/simulate.mjs",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "deploy": "npm run build && cp dist/game.js dist/style.css dist/manifest.json dist/index.html . && node scripts/update-sw-version.mjs && echo 'Deploy complete.'"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "terser": "^5.48.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vite-node": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
