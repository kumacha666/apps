{
  "name": "mori-no-yakai",
  "version": "0.1.22",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "node scripts/build.mjs",
    "preview": "vite preview",
    "deploy": "node scripts/update-sw-version.mjs && npm run build && cp dist/game.js dist/manifest.json . && echo 'Deploy complete.'"
  },
  "dependencies": {
    "firebase": "^11.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.3.0",
    "vitest": "^4.1.9"
  }
}
