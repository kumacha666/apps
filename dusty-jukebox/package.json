{
  "name": "dusty-jukebox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "prebuild": "npm test",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp dist/app.js . && node scripts/update-sw-version.mjs && echo 'Deploy complete. Commit and push to deploy.'"
  },
  "dependencies": {
    "music-metadata": "^10.6.4",
    "strtok3": "^10.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.9"
  }
}
