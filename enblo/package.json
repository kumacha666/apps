{
  "name": "enblo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "prebuild": "node scripts/restore-entry.mjs && npm test",
    "build": "vite build",
    "postbuild": "git checkout -- index.html",
    "sim": "npx vite-node scripts/simulate.mjs",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "predeploy": "node scripts/restore-entry.mjs",
    "deploy": "npm run build && cp dist/game.js dist/style.css dist/manifest.json dist/index.html . && node scripts/update-sw-version.mjs && echo 'Deploy complete.'"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "terser": "^5.48.0",
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vite-node": "^6.0.0",
    "vitest": "^4.1.9"
  }
}
