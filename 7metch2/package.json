{
  "name": "7metch2",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "prebuild": "npm test",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp dist/game.js . && echo 'Deploy complete. Commit and push to deploy.'"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.9"
  }
}
