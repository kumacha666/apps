# AGENTS.md — Codex実行契約（apps共通）

このファイルはCodex（実装エージェント）向けの固定ルールです。案件ごとの実装指示文はこのファイルの内容を前提とし、目的／対象／非対象／expected HEAD／Acceptance Criteria／Testsのみを記述します。

タスクの実装指示文がこのファイルと明示的に矛盾する内容を指示している場合は、実装指示文の指示を優先してください。

**ルートの`CLAUDE.md`は、対象アプリやアプリ固有AGENTS.mdの有無に関わらず、すべてのタスクで常に参照する。**Claude Code（設計・レビュー担当のAI）向けに書かれたファイルだが、Firebase Functionsの個別デプロイ手順（emoji-dm）やビルド成果物とソースの配置ルール等、まだ本ファイルへ移行していない実装・テスト・ビルド上の注意点を含む。加えて、対象アプリに固有の`CLAUDE.md`（存在する場合）も必ず参照・遵守する。

## 1. Source of Truth
- GitHub上のremote branch／PR／commit SHAを正とする。ローカルのコミットやサンドボックス内の状態は完了根拠にならない。
- 作業開始時に、指示された対象PR/branchのexpected HEADと、実際のremote HEADが一致するか確認する。一致しない場合はその旨を報告し、勝手に上書きしない。

## 2. 実装前のスコープ確認
- 新規・曖昧な仕様、認証・秘密情報、データ移行、セキュリティ、複数リポジトリ間の整合に関わるタスクは、Claude Code（設計担当）の判断を経てから実装に着手する前提で依頼される。
- 実装指示文にこれらの領域の設計方針が明記されていない、またはタスク内容がこれらに該当するにもかかわらず判断根拠が示されていない場合は、レビュー指摘として後から気づくのではなく、実装に着手する前にその旨を報告し、指示を確認する。

## 3. リポジトリへの反映経路
- サンドボックス内のgit/ghコマンドでのgit pushが常に成功するとは限らない。GitHub連携（コネクタ）によるリポジトリアクセスと、サンドボックス内のgit/ghによる書き込み能力は別物であり、同一視しない。
- 利用可能な書き込み経路をセッション開始時に確認する。既存PR/branchが指定されたタスクではそのbranchへ反映し、新規タスクでは指示されたbranch名で新しくbranchを作成して反映する。いずれも使える経路で行う。
- Promptや依頼文の書き方でpush権限を新たに獲得しようとしない。

## 4. 完了条件
- 完了条件は「ローカルでコミットした」ことではなく、「最終SHAがremote branchに実在すること」。
- 作業の最後に、remote branchの最新コミットSHAを確認し、報告する。

## 5. テスト・検証
- ゲームロジック・ビジネスロジックを変更する場合、該当箇所のユニットテストが存在することを確認してから変更する。存在しない場合は先にテストを書いてから変更する。「テスト通過」はテスト範囲の証明であって品質の証明ではない。
- 時系列・順序が絡むロジック（同時死亡・相討ち、判定の順序、処理中に対象が入れ替わる等）を「DOM操作を含むから」という理由だけでテスト対象外にしない。UI描画と切り離せる部分は関数として切り出し、必ずテストする。ハッピーパスの通しプレイだけでなく、こうした境界ケースを意図的に狙ったテストシナリオも用意する。
- モジュール分割・言語移行などの大規模リファクタリングは、対象がロジック変更に該当するか曖昧な場合でも、着手前にテスト・ガードレールを先に構築してから実施する。1PRに複数の大きな変更を詰め込まない。リファクタリング後は必ずビルド成果物の動作確認を行う。
- 実行できなかったテスト・検証（環境制約でスキップしたもの含む）を「成功」として報告しない。「not run」として明記する。
- 書き込み・副作用・イベント発火等を検証するテスト（例：「〜がSheetsに書き込まれた」「〜が呼び出された」ことを検証するテスト）を新規に追加する場合で、偽陽性のリスクがある（対象コードを削除・無効化してもテストが通ってしまう可能性がある）ときは、対象コードを一時的に無効化してテストが実際に失敗することを確認してから元に戻す（テストが偽陽性でないことの証明）。この確認は自己申告だけで終わらせず、再現可能な無効化手順（該当ファイル・変更内容）と失敗したアサーションの内容を最終報告に記録する。すべての新規テストに一律で要求するものではない。

## 6. デプロイ
- ビルド成果物をリポジトリのルート直下にコミットするアプリ（Vite+TypeScriptビルドアプリ全般）では、`npm run build`の成功だけを完了とせず、デプロイ用コマンド（例：`npm run deploy`。存在する場合はアプリ固有AGENTS.mdまたは`package.json`の`scripts`を確認する）まで実行し、ビルド成果物を最新化してからコミットする。アプリ固有AGENTS.mdがこの手順に例外・制約を定めている場合はそちらを優先する（「10. アプリ固有ルール」参照）。
- デプロイコマンド実行後、生成されたビルド成果物の差分（サイズ・内容）を確認する。コマンドが成功しても、変更した内容が実際に反映されていない・劣化したビルド（環境変数未設定等による機能欠落を含む）になっていないかを確認してからコミットする。

## 7. 環境依存能力（preflight）
実行時にブラウザ・ネットワーク・認証など環境依存の能力が使えるかを確認し、最終報告に以下の項目を具体的な値で記録する。「成功した」という結論だけでなく、何を確認できたかを残すこと。GitHubへの記録が可能な場合はPRにも残すが、記録の成否自体は完了条件にしない（GitHubへの書き込み能力自体が確認対象の一つであり、記録できたことを完了条件にすると循環するため）。

記録する項目：
- remote HEAD（確認時点のSHA）
- 対象アプリのAGENTS.mdおよびタスクで定義された、適用可能なtest/build/E2E結果（passed・failed・not runのいずれか。具体的なコマンド・項目はアプリ固有AGENTS.mdを参照）
- E2E対応アプリ（package.jsonのscriptsに`test:e2e`が存在するアプリ）では、Chromiumの有無（available・unavailable）、OS依存ライブラリの状態（available・unavailable。unavailableの場合は具体的な不足ライブラリまたはエラー）、および`npm run test:e2e`（またはビルド組み込みのE2Eゲート）の結果（passed・failed・not run。実行コマンド・exit code・主要なエラーを含む）
- git pushの結果（success・unavailable・authentication failed）
- remote branchへの反映に実際に使えた経路
- 作業完了時点のfinal remote HEAD（SHA）

## 8. E2E（実ブラウザ）テスト
- package.jsonのscriptsに`test:e2e`が存在するアプリを「E2E対応アプリ」とする。E2E対応アプリでは、Codex Cloud上で`npm run test:e2e`（またはビルド組み込みのE2Eゲート）を原則実行し、結果をpassed・failed・not runのいずれかで記録する。
- 「not run」は例外であり、コード側の問題ではなくCodex Cloud Environment側の障害で実行できない場合にのみ用いる。ChromiumまたはOS依存ライブラリ等の不足を、具体的なエラー内容・exit codeとともに記録する。E2Eがfailedの場合はfailedとして記録し、not runに置き換えない。
- CSS/DOM構造/アニメーションに関わる変更を含むタスクで、対象アプリがE2E対応アプリの場合は、既存スイートを実行するだけでなく、境界ケース（要素の重なりが起きやすい状態等）を実際にレンダリングして確認する検証を追加する。E2E非対応アプリで見た目に関わる変更をした場合は、その旨をPR本文に明記する。

## 9. PR運用
- ブランチ名はcodex/<topic>。
- 1PRに複数の大きな変更を詰め込まない。
- PR作成までを行い、mainへのマージは行わない。本リポジトリはpublicかつmainへのマージが直ちに本番デプロイになるため、マージは人間が実行する。人間がそのPRを名指しでマージを指示した場合に限り、軽微な修正（typo・ドキュメント等の自明な小変更）はsquash mergeしてよい。指示なしに自発的にマージしない。
- PR本文またはコメントに、適用可能なテスト結果とEnvironment evidenceを、対応する最終HEADのSHAと紐付けて記録する。
- レビュー指摘のうち、correctness／regression／data loss／security／テストの偽陽性偽陰性は必ず対応する。style/naming等の軽微な指摘は自身の修正提案機能で一次対応してよい。設計判断が要る指摘は提案のみ行い、実装は待つ。

## 10. アプリ固有ルール
- 各アプリディレクトリにAGENTS.mdがある場合はそちらを参照する。**アプリ固有のAGENTS.mdの内容が本ファイル（ルートAGENTS.md）と矛盾する場合は、そのアプリ固有の制約・例外を反映したものであるため、アプリ固有の内容を優先する**（例：通常はデプロイ必須でも、実クライアントID未設定の環境ではデプロイを避けるべきアプリがある）。
- アプリ固有のAGENTS.mdがまだ無い場合でも、そのアプリの`CLAUDE.md`（存在する場合）は引き続き必ず参照・遵守する。scoped AGENTS.mdはアプリのCLAUDE.mdを置き換えるものではなく、Codex向けの実行手順を追加するものである。
- 新しいアプリディレクトリ（`<app>/`）を追加した場合は、ルートの`CLAUDE.md`（リポジトリ構成・アプリ種別セクション）への反映と、テスト・ビルド構成があればアプリ固有`CLAUDE.md`の作成を行う。
