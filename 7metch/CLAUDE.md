# 7metch (ナナメッチ) — 開発ガイド

## テスト

### ユニットテスト (`npm test`)

- **フレームワーク**: Vitest (vite.config.js で設定)
- **テストファイル**: `src/board.test.js`
- **モック**: `src/__mocks__/audio.js` — SFX を no-op に差し替え (AudioContext 非依存化)
- **実行タイミング**: `npm run build` の prebuild で自動実行。テスト失敗時はビルドが中断される
- **対象**: board.js のコアロジック (isMatchable, findAllMatches, damageIce, getComboType, tickCountdowns 等)
- **前提**: ゲームロジックを変更した場合は必ずテストを追加・更新すること。特に isMatchable の判定条件を変えた場合はテストケースの追加が必須

### シミュレーションテスト (`npm run sim`)

- **スクリプト**: `scripts/simulate.mjs`
- **仕組み**: 描画なしでゲームロジックをヘッドレス実行。毎ターン有効な手からランダムに選択し、ステージごとの統計を収集する
- **収集指標**: クリア率、平均残り手数、平均スコア、平均消去数、詰み率、氷解除率、最大チェイン数
- **実行タイミング**: 任意。難易度調整やステージパラメータ変更後に実施する
- **オプション**:
  - `npm run sim` — デフォルト (主要18ステージ × 50回)
  - `npm run sim -- --stages 100-110` — ステージ範囲指定
  - `npm run sim -- --stages 100,200,300` — カンマ区切り
  - `npm run sim -- --runs 200` — 試行回数変更
  - `npm run sim -- --verbose` — 各ゲームの詳細出力
- **判定基準** (ランダムプレイ基準、人間はこれより大幅に上手い):
  - ✗ 極難: クリア率 5%未満
  - △ 難: クリア率 5-15%
  - ○ やや難: クリア率 15-30%
  - ◎: クリア率 30-80%
  - ○ 易: クリア率 80%超
  - ⚠詰: 詰み率 10%超
- **前提**: ランダムプレイの結果なので絶対値ではなく相対的なパターン (難易度カーブの滑らかさ、ステージ間の不連続) を重視する。全ステージで「✗ 極難」が出ないことが正常範囲の目安

## ビルド・デプロイ

- `npm run build` — テスト → ビルド → ポインタイベントチェック
- ビルド後は `dist/` の成果物をルートにコピーし、SW キャッシュバージョンを更新してからコミットする
- SW バージョンは `sw.js` と `public/sw.js` の両方を更新すること

## 難易度パラメータ (stages.js)

- **手数**: `max(14, 22 - tier)` + 氷ボーナス(+2) + CDボーナス(+1)
- **氷セル**: `1 + floor(progress * 3)` (最大4個、stage 100+)
- **岩セル**: `1 + floor(progress * 2)` (最大3個、stage 150+)
- **CDボム**: `1 + floor(progress * 1)` (最大2個、stage 300+)
- **ミッション倍率**:
  - クリア: `min(4.5, 2.5 + i * 0.01)` per move
  - スコア: `min(55, 30 + i * 0.2)` per move
  - 色消し: `min(0.8, 0.4 + i * 0.005)` per move
- パラメータを変更した場合はシミュレーションテストで難易度カーブを確認すること

## 次セッションのタスク

### 1. TypeScript 移行 (Phase 6 の前に実施)

- **目的**: 引数不足・型不整合などのバグをコンパイル時に検出する（今セッションで getMissionText の引数不足バグがあった）
- **規模**: 15ファイル、約3000行。1-2セッションで完了見込み
- **方針**:
  1. `tsconfig.json` を追加 (`allowJs: true` で段階的移行を許可)
  2. Vite は標準で TS 対応のためビルド設定変更は最小限
  3. 型の恩恵が大きいファイルから順に `.js` → `.ts` に変換:
     - `state.ts` (G オブジェクト・定数の型定義)
     - `stages.ts` (ステージ生成・ミッション型)
     - `board.ts` (コアロジック)
     - 残りのファイル
  4. 全ファイル移行後、`strict: true` に設定
  5. ユニットテスト (`npm test`) とシミュレーション (`npm run sim`) が通ることを確認
  6. `/deploy-7metch` でデプロイ
- **注意**: テストファイル (`board.test.js`) とシミュレーション (`scripts/simulate.mjs`) も `.ts` に変換すること

### 2. /deploy-7metch スキルの動作確認

- 今セッションで作成したスキル (`.claude/skills/deploy-7metch.md`)
- 次セッションで `/deploy-7metch` がTab補完で使えるか確認する
- 使えない場合はスキルの配置やファイル名を調整する

### 3. SPEC ドキュメント更新

- `ai-workspace/projects/7metch/SPEC.md` と `SPEC_DEV.md` が v6.6.0 時点で止まっている（現在 v7.5.8）
- 主な差分: ステージ数(350→500)、手数計算式、氷/CDの仕様変更(マッチ不可に)、ミッション倍率調整、getMissionIconUrl削除、スターゲート数(9→11)、Vite/テスト/チュートリアル情報の追加
- TypeScript 移行と並行して更新する

### 4. Phase 6: マネタイズ等の機能追加

- TypeScript 移行完了後に着手
- 具体的な内容は次セッションで相談
