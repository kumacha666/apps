{
  "name": "7metch",
  "version": "7.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "prebuild": "npm test && node scripts/fix-entry.mjs",
    "build": "vite build && node --input-type=module -e \"import{readFileSync}from'fs';const j=readFileSync('dist/game.js','utf8');const m=['pointerdown','pointerup','pointermove'].filter(s=>!j.includes(s));if(m.length){console.error('BUILD CHECK FAILED: missing: '+m.join(', '));process.exit(1)}console.log('Build check passed.')\"",
    "sim": "node scripts/simulate.mjs",
    "preview": "vite preview",
    "deploy": "npm run build && cp dist/game.js dist/index.html dist/style.css dist/manifest.json . && echo 'Deploy files copied to root. Run: git checkout index.html  to restore dev mode.'"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "vite": "^6.3.0",
    "vitest": "^4.1.9"
  }
}
