{
  "name": "7metch",
  "version": "7.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cp dist/game.js dist/index.html dist/style.css dist/manifest.json . && echo 'Deploy files copied to root'"
  },
  "devDependencies": {
    "terser": "^5.48.0",
    "vite": "^6.3.0"
  }
}
